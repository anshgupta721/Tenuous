[package]
name = "pyfrontend"
version = "0.1.0"
edition = "2024"

[dependencies]
dynamics = { path = "../dynamics" }
estimator = { path = "../estimator" }
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"